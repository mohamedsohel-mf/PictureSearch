export const loading = state => state.loading

export const photos = state => state.photos

export const currentPage = state => state.currentPage

export const allPages = state => state.allPages

export const perPage = state => state.perPage

export const locationsInfo = state => state.locationsInfo